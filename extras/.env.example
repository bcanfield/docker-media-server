# Homepage allowed hosts (hostname or IP used to access the dashboard, may need port)
HOMEPAGE_ALLOWED_HOSTS=localhost:3000

# Jellystat (Jellyfin monitoring)
JELLYSTAT_DB_PASSWORD=changeme
JELLYSTAT_JWT_SECRET=changeme-generate-a-long-random-string
